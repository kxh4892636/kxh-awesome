---
name: handoff
description: 创建或更新当前会话的交接文档，保存到当前项目的 docs/handoff/{sessionID}.md。用户要求 handoff、交接、交接文档、会话交接、更新上下文文档，compact/resume 注入提示要求立刻使用 handoff skill，或当前对话/交接文档存在 TaskList 且任务状态、关键改动、验证结果、commit、阻塞项发生变化时，必须使用本 skill。
---

# Handoff

## 执行时机

- 用户手动要求创建或更新交接文档时，使用本 skill。
- compact/resume 注入上下文要求“immediately use the handoff skill”时，立刻使用本 skill。
- 如果当前对话或交接文档中存在 TaskList，则启用自动更新机制：任一 TaskList 任务完成、阻塞或状态变化后，必须立即更新交接文档。
- 如果不存在 TaskList，不启用自动更新机制；只有用户再次手动调用本 skill 时才创建或更新交接文档。

## 自动更新触发点

存在 TaskList 时，下列变化都需要更新交接文档，因为接手模型需要看到最新事实，而不是只看到过期计划：

- TaskList 任一任务进入 `in_progress`、`completed` 或 `blocked`，或从阻塞恢复。
- 用户追加、收窄或改变目标、完成标准、不做范围或优先级。
- 新增、修改、删除关键文件，或完成一组可交付改动。
- 执行验证命令并得到通过、失败、跳过或未验证结论。
- 产生 git stage、commit、push、PR，或确认暂存区/工作区范围。
- 发现新的风险、阻塞、未确认项、环境限制或外部依赖。
- compact/resume 后上下文从交接文档恢复，且当前实际状态与文档不一致。

## 路径规则

- 交接文档必须保存到当前项目的 `docs/handoff/{sessionID}.md`。
- 所有写入文档中的文件路径必须使用绝对路径。

## 工作流程

1. 确定当前项目根目录、sessionID 和交接文档绝对路径；如果当前目录在 Git 仓库内，以 Git 根目录作为项目根目录。
2. 如果交接文档已存在，先读取现有内容，再更新；禁止盲目覆盖仍然有效信息，执行增量更新操作，只删除确定失效信息。
3. 梳理最新用户意图、已验证事实、假设、决策、状态、TaskList、验证结果、风险和下一步。
4. 事实和假设必须分开写。无法验证但影响执行的内容，使用 `未确认` 标记。
5. 更新或创建 `docs/handoff/{sessionID}.md`。目录不存在时先创建目录。
6. 如果存在 TaskList，后续每完成任一 TaskList 任务，都把交接文档更新作为该任务完成前的最后一步，更新包含对应任务改动涉及的所有内容。

## 字段更新规则

- `目标`：用户改变目标、完成标准或不做范围时更新；不要把旧目标和新目标混在一起。
- `最新用户意图`：记录用户最近明确要求，尤其是会改变执行路径的补充指令。
- `当前上下文`：记录工作目录、相关文件、当前分支、运行环境和时间信息；文件路径使用绝对路径。
- `已确认事实`：只写已经通过文件读取、命令输出、用户明确说明或工具结果验证过的信息。
- `假设与未确认项`：写无法验证但会影响执行的内容，并用 `未确认` 标记。
- `已做决策`：写决策、原因和影响范围，避免接手模型重复推翻已经确认的路径。
- `当前状态`：同步已完成、正在进行、尚未开始和被阻塞的事项。
- `TaskList`：每个任务必须带状态、证据和下一步；状态只使用 `pending`、`in_progress`、`completed`、`blocked`。
- `关键改动/执行命令`：记录关键文件、命令和结果摘要；只保留能帮助继续执行的信息。
- `验证情况`：明确哪些验证通过、失败、跳过或未执行；未验证必须写原因。
- `风险与注意事项`：记录最容易导致返工、误删、误提交、环境失败或理解偏差的点。
- `下一步`：按接手模型可以直接执行的顺序写，避免笼统描述。

## 内容约束

- 交接文档的完成标准是：接手模型不读取聊天记录，也能知道目标、事实、当前状态、风险和下一步。
- 必须保证内容的时效性，与最新上下文一致；不要只更新 TaskList 状态而遗漏证据、验证结果或下一步。
- 保留仍有效的旧信息，删除或改写已经失效的信息；不要盲目整篇覆盖。
- 不把推理过程大段塞进文档，只保留能指导继续执行的结论、证据和决策。
- 不把未验证猜测写成事实。
- 不记录敏感原文、凭据、密钥、账号或不必要的个人信息。

## 文档模板

```md
# 会话交接文档

## 1. 目标

## 2. 最新用户意图

## 3. 当前上下文

## 4. 已确认事实

## 5. 假设与未确认项

## 6. 已做决策

## 7. 当前状态

## 8. TaskList

| ID  | 任务 | 状态    | 证据 | 下一步 |
| --- | ---- | ------- | ---- | ------ |
| T1  |      | pending |      |        |
| T2  |      | pending |      |        |

状态只使用：`pending` / `in_progress` / `completed` / `blocked`。

## 9. 关键改动/执行命令

## 10. 验证情况：

## 11. 风险与注意事项

## 12. 下一步

## 13. 参考资源
```
